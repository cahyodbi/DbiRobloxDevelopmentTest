# Policies

Contains business logic gates, validation rules, and authorization checks.

## Purpose
- Encapsulates "rules of the game" (e.g., "can player A buy item B?").
- Separates validation logic from primary service orchestration.
- Centralizes decision-making rules to ensure consistency across the codebase.
