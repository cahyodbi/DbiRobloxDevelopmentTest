# Server Interfaces

Definitions of contracts and API expectations for server-side components.

## Purpose
- Enforces a clear separation between the "what" (interface) and the "how" (implementation).
- Allows for swappable implementations (e.g., swapping a real DataStore repository for a mock one during testing).
