# Shared Constants

Central location for global configuration, static values, and immutable data.

## Purpose
- Avoids the use of "magic numbers" or hardcoded strings throughout the project.
- Provides a single source of truth for configuration values used by both client and server.
